version: 2.1

jobs:
  build2:
    docker:
      - image: cimg/ruby:3.1.0
    steps:
      - checkout
      - run:
          name: print test
          command: echo "new workflow"

workflows:
  new_workflow: # This is the name of the workflow, feel free to change it to better match your workflow.
    jobs:
      - build2

